/**
 * @deprecated Use `import { motorClient, processService, taskService } from 'bpms-frontend-master'` instead.
 * Mantido apenas para compatibilidade - todos os services agora ficam na lib modulos/frontend.
 * Sera removido em proxima versao.
 */

export { motorClient as motorApi } from 'bpms-frontend-master'
export { processService } from 'bpms-frontend-master'
export { taskService } from 'bpms-frontend-master'
